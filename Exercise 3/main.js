// Variable declarations
let isSunny = true;
let isWeekend = true;

// Logical AND (&&) - Both conditions must be true to go to the beach
let canGoToBeach = isSunny && isWeekend;

// Log the result
console.log("Can I go to the beach?", canGoToBeach); // Output: true